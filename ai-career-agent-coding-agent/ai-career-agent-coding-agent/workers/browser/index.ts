import dns from 'node:dns/promises';
import net from 'node:net';
import { chromium } from 'playwright';
const BLOCKED_HOSTS=new Set(['localhost','localhost.localdomain','metadata.google.internal']);
function privateIp(ip:string){if(net.isIP(ip)===4){const [a,b]=ip.split('.').map(Number);return a===10||a===127||a===0||(a===169&&b===254)||(a===172&&b>=16&&b<=31)||(a===192&&b===168);}const normalized=ip.toLowerCase();return normalized==='::1'||normalized==='::'||normalized.startsWith('fc')||normalized.startsWith('fd')||normalized.startsWith('fe80:');}
export async function assertSafeNavigation(raw:string,allowedDomains:string[]=[]){const url=new URL(raw);if(url.protocol!=='https:'&&url.protocol!=='http:')throw new Error('UNSUPPORTED_URL_SCHEME');if(url.username||url.password||BLOCKED_HOSTS.has(url.hostname.toLowerCase()))throw new Error('SSRF_BLOCKED');if(allowedDomains.length&&!allowedDomains.some(d=>url.hostname===d||url.hostname.endsWith(`.${d}`)))throw new Error('DOMAIN_NOT_ALLOWLISTED');const addresses=await dns.lookup(url.hostname,{all:true,verbatim:true});if(!addresses.length||addresses.some(a=>privateIp(a.address)))throw new Error('SSRF_BLOCKED');return url;}
export async function runBrowserTask(raw:string,allowedDomains:string[]=[]){const initial=await assertSafeNavigation(raw,allowedDomains);const browser=await chromium.launch({headless:true});try{const context=await browser.newContext();const page=await context.newPage();await page.route('**/*',async route=>{const request=route.request();if(request.isNavigationRequest()){try{await assertSafeNavigation(request.url(),allowedDomains);return route.continue();}catch{return route.abort();}}return route.continue();});const response=await page.goto(initial.toString(),{waitUntil:'domcontentloaded',timeout:30_000});const finalUrl=await assertSafeNavigation(page.url(),allowedDomains);return {title:await page.title(),url:finalUrl.toString(),status:response?.status()??null};}finally{await browser.close();}}
