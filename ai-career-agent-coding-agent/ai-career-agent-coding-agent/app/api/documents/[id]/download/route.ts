import { NextResponse } from 'next/server'; import { requireUser } from '@/lib/auth'; import { supabaseAdmin } from '@/lib/supabase';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const user=await requireUser();const {id}=await params;const {data}=await supabaseAdmin.from('documents').select('storage_path,original_name').eq('id',id).eq('user_id',user.id).maybeSingle();if(!data)return NextResponse.json({error:'NOT_FOUND'},{status:404});const {data:signed,error}=await supabaseAdmin.storage.from('career-documents').createSignedUrl(data.storage_path,60);if(error||!signed)return NextResponse.json({error:'DOWNLOAD_UNAVAILABLE'},{status:500});return NextResponse.json({url:signed.signedUrl,name:data.original_name});}
