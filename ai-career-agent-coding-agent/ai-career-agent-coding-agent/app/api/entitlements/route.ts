import {requireUser} from '../../../lib/auth';import {getEntitlement} from '@packages/security/entitlements';export async function GET(req: Request){const u=await requireUser({ req: req }).catch(()=>null);if(!u)return Response.json({error:'UNAUTHENTICATED'},{status:401});return Response.json(await getEntitlement(u.id))}
