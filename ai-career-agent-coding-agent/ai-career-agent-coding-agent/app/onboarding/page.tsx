'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function Onboarding(){const router=useRouter();const [name,setName]=useState('');const [roles,setRoles]=useState('');const [headline,setHeadline]=useState('');const [summary,setSummary]=useState('');const [skills,setSkills]=useState('');const [status,setStatus]=useState('');async function save(e:React.FormEvent){e.preventDefault();setStatus('Saving your workspace…');const r=await fetch('/api/profile',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({full_name:name,target_roles:roles.split(',').map(x=>x.trim()).filter(Boolean),headline,summary,skills:skills.split(',').map(x=>x.trim()).filter(Boolean)})});if(!r.ok){setStatus('Please add your name and at least one target role.');return;}router.push('/dashboard');router.refresh();}return <main className="auth-page"><section className="onboarding-card"><p className="eyebrow">YOUR CAREER WORKSPACE</p><h1>Start with what’s true.</h1><p className="muted">Your profile is the source of facts Modern Job can use. You can refine it whenever you want.</p><form onSubmit={save} className="form-stack"><label>Full name<input value={name} onChange={e=>setName(e.target.value)} required placeholder="Your name"/></label><label>Target roles <span>Separate multiple roles with commas</span><input value={roles} onChange={e=>setRoles(e.target.value)} required placeholder="e.g. Product Designer, UX Researcher"/></label><label>Professional headline<input value={headline} onChange={e=>setHeadline(e.target.value)} placeholder="What you do, in your own words"/></label><label>Short professional summary<textarea value={summary} onChange={e=>setSummary(e.target.value)} rows={4} placeholder="Share the experience you want your agent to understand."/></label><label>Skills <span>Optional, comma-separated</span><input value={skills} onChange={e=>setSkills(e.target.value)} placeholder="e.g. Figma, user research, workshops"/></label><button className="btn" type="submit">Create my workspace</button>{status&&<p className="form-status">{status}</p>}</form></section></main>}
