'use client';
import {useState} from 'react';
export default function CredentialForm(){const [userId,setUserId]=useState('');const [provider,setProvider]=useState<'openrouter'|'huggingface'>('openrouter');const [apiKey,setApiKey]=useState('');const [model,setModel]=useState('openai/gpt-4o-mini');const [baseUrl,setBaseUrl]=useState('https://openrouter.ai/api/v1');const [message,setMessage]=useState('');async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/credentials',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({userId,provider,apiKey,model,baseUrl})});const j=await r.json();setMessage(j.ok?'Saved encrypted credential.':j.error??'Failed');setApiKey('')}return <form onSubmit={save} className="grid card" style={{maxWidth:700}}><input placeholder="User UUID" value={userId} onChange={e=>setUserId(e.target.value)} required/><select value={provider} onChange={e=>{const p=e.target.value as any;setProvider(p);setBaseUrl(p==='openrouter'?'https://openrouter.ai/api/v1':'https://router.huggingface.co/v1')}}><option value="openrouter">OpenRouter</option><option value="huggingface">Hugging Face</option></select><input placeholder="API key" type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} required/><input placeholder="Model" value={model} onChange={e=>setModel(e.target.value)} required/><input placeholder="Base URL" value={baseUrl} onChange={e=>setBaseUrl(e.target.value)} required/><button className="btn">Save encrypted key</button>{message&&<p>{message}</p>}</form>}
