import {requireUser} from '../../../lib/auth';import {supabaseAdmin} from '../../../lib/supabase';
export default async function AdminUsers(){const u=await requireUser();const {data:admin}=await supabaseAdmin.from('admin_users').select('user_id').eq('user_id',u.id).maybeSingle();if(!admin)return <main className="shell"><h1>Forbidden</h1></main>;const {data}=await supabaseAdmin.from('admin_user_overview').select('*').order('created_at',{ascending:false});return <main className="shell"><h1>Admin • Users</h1><p className="muted">Review duplicate-risk signals, plans and account status. Termination is server-side and audited.</p><div className="grid">{(data??[]).map((x:any)=><div className="card" key={x.user_id}><strong>{x.email}</strong><div>{x.plan} • {x.account_status}</div><small>risk: {x.risk_score??0}</small></div>)}</div></main>}
