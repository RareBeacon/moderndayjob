import Link from 'next/link'; import { redirect } from 'next/navigation';
import { requireUser } from '@/lib/auth'; import { supabaseAdmin } from '@/lib/supabase'; import { getEntitlement } from '@packages/security/entitlements';
export default async function Dashboard(){const user=await requireUser();const [{data:profile},{data:career},entitlement]=await Promise.all([supabaseAdmin.from('profiles').select('full_name,target_roles,account_status').eq('user_id',user.id).single(),supabaseAdmin.from('career_profiles').select('headline,skills').eq('user_id',user.id).maybeSingle(),getEntitlement(user.id)]);if(!profile?.target_roles?.length)redirect('/onboarding');const [{count:applicationCount},{count:taskCount}]=await Promise.all([supabaseAdmin.from('applications').select('*',{count:'exact',head:true}).eq('user_id',user.id),supabaseAdmin.from('agent_tasks').select('*',{count:'exact',head:true}).eq('user_id',user.id).in('status',['QUEUED','RUNNING'])]);return <main className="workspace"><header className="workspace-header"><Link href="/" className="brand">modernjob</Link><form action="/api/auth/signout" method="post"><button className="text-button">Sign out</button></form></header><section className="workspace-hero"><p className="eyebrow">YOUR WORKSPACE</p><h1>Welcome, {profile.full_name}.</h1><p>{career?.headline||'Your private career workspace is ready. Build your profile, choose your tools, and move at your own pace.'}</p><Link className="btn" href="/onboarding">Edit profile</Link></section><section className="metrics"><article><p>Plan</p><strong>{entitlement.plan}</strong><span>{entitlement.automation_enabled?'Automation available':'Automation locked'}</span></article><article><p>AI credits today</p><strong>{entitlement.ai_credits_remaining}</strong><span>Available now</span></article><article><p>Applications</p><strong>{applicationCount||0}</strong><span>Real records only</span></article><article><p>Agent tasks</p><strong>{taskCount||0}</strong><span>Queued or running</span></article></section><section className="workspace-grid"><article className="card"><p className="eyebrow">YOUR DIRECTION</p><h2>{profile.target_roles.join(' · ')}</h2><p className="muted">{career?.skills?.length?`${career.skills.length} skills in your verified profile.`:'Add skills and experience to improve future recommendations.'}</p><div className="dashboard-links"><Link href="/onboarding" className="inline-link">Update profile →</Link><Link href="/documents" className="inline-link">Upload CV →</Link></div></article><article className="card"><p className="eyebrow">NEXT STEP</p><h2>Build your career foundation.</h2><p className="muted">CV upload, free AI career tools, and job matching are being connected to this workspace next. Nothing is submitted automatically.</p><div className="dashboard-links"><Link href="/applications" className="inline-link">Track applications →</Link><Link href="/billing" className="inline-link">View plans and limits →</Link></div></article></section></main>}
