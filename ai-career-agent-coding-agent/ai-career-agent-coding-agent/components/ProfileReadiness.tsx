'use client'; import {useEffect,useState} from 'react'; export default function ProfileReadiness(){const [x,setX]=useState<{percent:number;next:string[]}|null>(null);useEffect(()=>{fetch('/api/profile/completeness').then(r=>r.json()).then(setX)},[]);if(!x)return <p className="muted">Checking your profile…</p>;return <><div className="progress"><i style={{width:`${x.percent}%`}}/></div><p><b>{x.percent}% complete.</b> {x.next.length?`Next: ${x.next.slice(0,2).join(' · ')}`:'Your profile foundation is complete.'}</p></>}
