{
  "name": "jobiest-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Jobiest native app shells (Android + iOS) via Capacitor. The product is the web app; these shells wrap https://jobiest.com with native splash, status bar and share handling.",
  "scripts": {
    "bootstrap": "npm install && npx cap add android && npx cap add ios || true",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  }
}
