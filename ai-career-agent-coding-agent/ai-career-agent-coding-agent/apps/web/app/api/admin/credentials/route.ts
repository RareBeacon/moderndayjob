import {requireUser} from '@/lib/auth';import {supabaseAdmin} from '@/lib/supabase';import {encryptSecret} from '@packages/security/crypto';import {z} from 'zod';
const body=z.object({userId:z.string().uuid(),provider:z.enum(['openrouter','huggingface']),apiKey:z.string().min(10).max(500),model:z.string().min(1).max(200),baseUrl:z.string().url()});
export async function POST(req:Request){const admin=await requireUser();const {data:a}=await supabaseAdmin.from('admin_users').select('user_id').eq('user_id',admin.id).maybeSingle();if(!a)return Response.json({error:'FORBIDDEN'},{status:403});const b=body.parse(await req.json());const ciphertext=encryptSecret(b.apiKey);const {error}=await supabaseAdmin.from('ai_credentials').insert({user_id:b.userId,provider:b.provider,model:b.model,base_url:b.baseUrl,ciphertext,status:'ACTIVE'});if(error)return Response.json({error:error.message},{status:409});await supabaseAdmin.from('admin_actions').insert({admin_user_id:admin.id,target_user_id:b.userId,action:'ADD_AI_CREDENTIAL',metadata:{provider:b.provider,model:b.model}});return Response.json({ok:true})}
