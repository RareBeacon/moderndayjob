import {requireUser} from '../../../lib/auth';import {getEntitlement} from '@packages/security/entitlements';
export default async function Dashboard(){const u=await requireUser();const e=await getEntitlement(u.id);return <main className="shell"><span className="badge">{e.plan}</span><h1>Welcome back</h1><p className="muted">{u.email}</p><div className="grid grid3"><div className="card"><h3>AI credits</h3><strong>{e.ai_credits_remaining}</strong><p className="muted">remaining today</p></div><div className="card"><h3>Applications</h3><strong>{e.applications_remaining}</strong><p className="muted">remaining today</p></div><div className="card"><h3>Automation</h3><strong>{e.automation_enabled?'Enabled':'Locked'}</strong></div></div></main>}
