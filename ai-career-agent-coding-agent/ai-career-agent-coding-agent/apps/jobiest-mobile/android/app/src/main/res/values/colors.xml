<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Warm petrol used by the app UI surfaces (current design-system token
         --ink: #14201f from app/globals.css). -->
    <color name="jobiest_ink">#14201F</color>
    <!-- Petrol used by the brand artwork itself (sampled from the app icon),
         so the adaptive launcher background matches the legacy icon exactly. -->
    <color name="jobiest_icon_ink">#0C2A2E</color>
    <!-- --brand: #0ba5a0 (current token; the artwork uses #0aa9a6). -->
    <color name="jobiest_brand">#0BA5A0</color>
</resources>
