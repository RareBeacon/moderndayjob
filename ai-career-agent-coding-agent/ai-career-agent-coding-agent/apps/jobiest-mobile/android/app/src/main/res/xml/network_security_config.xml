<?xml version="1.0" encoding="utf-8"?>
<!-- Production traffic is HTTPS-only. Cleartext is permitted exclusively for
     the host loopback addresses used when a developer points the app at a
     locally running Next.js backend (adb reverse / Android emulator). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
